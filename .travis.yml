dist: xenial   # required for Python >= 3.7
language: python
python:
  - "3.7"
  #- "nightly"  # nightly build
install:
  - pip install -r requirements.txt
# command to run tests
script: 
  - pytest -q ./datapungibea/tests/test_drivers.py --cmdopt=$pyTvar --maxfail=2
  
  
